const solution = require('../{{camelCase name}}');

describe('', () => {
  it('should be ', () => {
    expect(solution())
  });
  
  it('should be ', () => {
    expect(solution())
  });
  
  it('should be ', () => {
    expect(solution())
  });
})